# 基于边缘计算的绝缘子缺陷检测系统 — 项目使用说明

**学生：王新壹 | 专业：电子信息工程 | 学号：202205040106**

---

## 一、项目概述

本项目设计并实现了一套完整的**边缘-云端协同绝缘子缺陷检测系统**。核心思路是：

- **云端**：用 YOLOv8n + 注意力机制(CBAM/ECA/LSKA/CA) + P2小目标检测层训练高精度模型
- **边缘端**：将模型轻量化(GhostConv+剪枝)后导出 ONNX/TensorRT，部署到 NVIDIA Jetson Nano
- **GUI桌面端**：PyQt5 开发的检测系统，支持单张/批量/视频/摄像头检测
- **协同**：边缘端反馈 → 云端增量训练 → 模型发布 → 边缘端热加载

---

## 二、目录结构一览

```
insulator_defect_coop/
├── window_main.py              ← 【GUI主程序】启动桌面检测系统
├── yolov8n.pt                  ← YOLOv8n 预训练权重（约6.5MB）
├── requirements.txt            ← Windows 开发环境依赖
├── requirements-jetson.txt     ← Jetson Nano 边缘端依赖
├── 毕设任务.txt                 ← 毕设任务书 + 改进记录
├── 项目使用说明.md              ← 本文件
│
├── cloud/                      ← 【云端训练层】
│   ├── train_better.py         ←   ★ 核心训练脚本（配置开关在此修改）
│   ├── cloud_api.py            ←   Flask API（反馈接收、模型管理）
│   ├── model_registry.py       ←   SQLite 模型版本管理
│   ├── dataset_analysis.py     ←   数据集分析工具（类别分布、尺寸分析）
│   ├── auto_train.py           ←   定时自动训练调度
│   ├── insulator.yaml          ←   4类数据集配置
│   ├── insulator_5class.yaml   ←   5类数据集配置（含污秽）
│   ├── yolo26n.pt              ←   YOLOv6n 对比实验权重
│   ├── yolov8n.yaml            ←   标准 YOLOv8n 架构
│   ├── yolov8n-lska-ca.yaml    ←   LSKA+CA 注意力架构
│   ├── yolov8n-ghost.yaml      ←   GhostConv 轻量化架构
│   ├── yolov8n-p2.yaml         ←   P2小目标检测层架构（160×160）
│   ├── yolov8n-bifpn.yaml      ←   BiFPN 双向特征金字塔架构
│   ├── data/                   ←   ★ 数据集（约1.3GB）
│   │   ├── images/train/       ←   训练集图片（2100张）
│   │   ├── images/val/         ←   验证集图片（601张）
│   │   ├── images/test/        ←   测试集图片（300张）
│   │   ├── labels/train/       ←   训练集标注（YOLO格式）
│   │   ├── labels/val/         ←   验证集标注
│   │   ├── labels/test/        ←   测试集标注
│   │   └── classes.txt         ←   类别名称列表
│   ├── data_collector/         ←   多源数据采集模块
│   │   ├── __init__.py
│   │   ├── capture.py          ←   无人机+固定摄像机双源采集
│   │   └── normalize.py        ←   图像归一化（分辨率/色彩/命名）
│   ├── modules/                ←   ★ 自定义模块库
│   │   ├── __init__.py         ←   模块注册到Ultralytics命名空间
│   │   ├── lska.py             ←   LSKA 大核可分离注意力
│   │   ├── ca.py               ←   CA 坐标注意力
│   │   ├── cbam.py             ←   CBAM 通道+空间混合注意力
│   │   ├── eca.py              ←   ECA 高效通道注意力
│   │   ├── ghostconv.py        ←   GhostConv 轻量化卷积
│   │   ├── asff.py             ←   ASFF 自适应空间特征融合
│   │   └── losses.py           ←   WIoU/Focal Loss/Slide Loss
│   └── runs/                   ←   云端训练输出目录
│
├── edge/                       ← 【边缘推理层】
│   ├── edge_inference.py       ←   ★ 边缘推理引擎（主入口）
│   ├── trt_inference.py        ←   TensorRT FP16/INT8 推理引擎
│   ├── benchmark.py            ←   推理性能基准测试工具
│   ├── config.py               ←   边缘端配置（类别、路径、阈值）
│   ├── utils.py                ←   边缘端工具函数
│   └── models/                 ←   云端发布的模型文件
│       └── current.onnx        ←   当前激活的 ONNX 模型（约12MB）
│
├── deploy/                     ← 【Jetson 部署工具】
│   ├── setup_jetson.sh         ←   Jetson 环境一键安装脚本
│   ├── deploy.sh               ←   一键部署到 Jetson 脚本
│   └── insulator-detect.service←   systemd 开机自启服务
│
├── runs/                       ← 【训练实验结果存档】（约177MB）
│   └── insulator_defect_*/     ←   每次训练的完整输出
│
├── 文献/                       ← 【参考文献PDF】（16篇）
│
├── 基于边缘计算的绝缘子缺陷检测系统_improved.docx       ← 毕设报告（原始改进版）
└── 基于边缘计算的绝缘子缺陷检测系统_improved_增强版.docx ← 毕设报告（最终增强版）
```

---

## 三、每个文件详细说明

### 3.1 根目录

| 文件 | 一句话说明 | 什么时候用 |
|------|-----------|-----------|
| `window_main.py` | GUI检测系统主程序，双击运行 | 演示/测试检测效果 |
| `yolov8n.pt` | YOLOv8n预训练权重 | 训练时自动加载，不要删除 |
| `requirements.txt` | `pip install -r requirements.txt` | 配置Windows开发环境 |
| `requirements-jetson.txt` | Jetson专用依赖 | 配置Jetson Nano环境 |

### 3.2 cloud/ — 云端训练层

| 文件 | 核心作用 | 关键配置/用法 |
|------|---------|--------------|
| **`train_better.py`** | **主训练脚本**，运行即开始训练 | 修改文件顶部的 `CFG` 字典切换架构/注意力/损失函数 |
| `cloud_api.py` | Flask API 服务器 | `python cloud_api.py` 启动，监听5000端口 |
| `model_registry.py` | SQLite数据库，记录每次训练的指标 | 被 train_better 和 cloud_api 调用 |
| `dataset_analysis.py` | 分析数据集类别分布、边界框尺寸 | `python dataset_analysis.py` 查看数据报告 |
| `auto_train.py` | 定时自动训练调度器 | 配置定时任务后自动触发增量训练 |
| `insulator.yaml` | 4类数据集路径和类别定义 | 训练脚本读取此文件定位数据 |
| `insulator_5class.yaml` | 5类数据集配置（含污秽类别） | 污秽数据集就绪后切换使用 |

**模型架构YAML（选哪个？）**

| YAML文件 | 架构特点 | 适用场景 |
|---------|---------|---------|
| `yolov8n.yaml` | 标准YOLOv8n，无改动 | 基线对比实验 |
| `yolov8n-lska-ca.yaml` | LSKA大核注意力 + CA坐标注意力 | 通用精度提升 |
| `yolov8n-p2.yaml` | **P2检测层(160×160)** + CBAM | ★ 小目标（闪络）检测，推荐 |
| `yolov8n-bifpn.yaml` | BiFPN跨层融合 + ECA注意力 | 多尺度特征融合 |
| `yolov8n-ghost.yaml` | GhostConv轻量化卷积 | 极致轻量化场景 |

**自定义模块库 `modules/`**

| 文件 | 模块名 | 参数量 | 作用 |
|------|-------|--------|------|
| `lska.py` | LSKA | 轻量 | 大核可分离注意力，增强小目标感受野 |
| `ca.py` | CA | 极轻 | 坐标注意力，嵌入位置信息 |
| `cbam.py` | CBAM | 中 | 通道+空间双注意力，综合效果最好 |
| `eca.py` | ECA | 极轻 | 1D卷积通道注意力，几乎无额外参数 |
| `ghostconv.py` | GhostConv | 减半 | 轻量化卷积，参数量降约50% |
| `asff.py` | ASFF | 轻量 | 自适应空间特征融合，学习各层权重 |
| `losses.py` | WIoU/Focal/Slide | 无 | 改进损失函数集合 |

**数据采集 `data_collector/`**

| 文件 | 作用 |
|------|------|
| `capture.py` | 无人机(RTSP/视频) + 固定摄像机(USB/RTSP) 双源采集 |
| `normalize.py` | 分辨率统一、自动白平衡、命名规范化 |

### 3.3 edge/ — 边缘推理层

| 文件 | 核心作用 | 关键特性 |
|------|---------|---------|
| **`edge_inference.py`** | **边缘推理引擎主入口** | 自动后端选择(TensorRT→ONNX-CUDA→ONNX-CPU)、模型热加载、多尺度推理、反馈采集上传 |
| `trt_inference.py` | TensorRT推理引擎 | FP16/INT8量化、推理预热、批量推理、性能统计 |
| `benchmark.py` | 基准性能测试 | 对比ONNX vs TensorRT的FPS/延迟/吞吐量 |
| `config.py` | 边缘端配置 | 模型路径、类别映射、缓存目录、推理参数 |
| `utils.py` | 工具函数 | 字母框缩放、NMS、结果保存等 |

### 3.4 deploy/ — Jetson部署工具

| 文件 | 作用 |
|------|------|
| `setup_jetson.sh` | 一键配置Jetson Nano环境（apt依赖+pip包） |
| `deploy.sh` | 一键部署：同步代码→安装依赖→配置服务→启动自启 |
| `insulator-detect.service` | systemd服务文件，开机自动启动边缘推理 |

### 3.5 报告文件

| 文件 | 内容 |
|------|------|
| `基于边缘计算的绝缘子缺陷检测系统_improved.docx` | 原始改进版报告（7章，254段） |
| `基于边缘计算的绝缘子缺陷检测系统_improved_增强版.docx` | **最终增强版**（含6张实验表格、技术深度阐述） |

---

## 四、操作指南

### 4.1 环境配置

```bash
# Windows 开发环境
conda create -n pytorch python=3.10
conda activate pytorch
pip install -r requirements.txt

# Jetson Nano 边缘端
scp deploy/setup_jetson.sh jetson@<IP>:~/
ssh jetson@<IP>
bash setup_jetson.sh
```

### 4.2 训练模型

```bash
cd cloud
python train_better.py
```

**训练前修改 `train_better.py` 中 `CFG` 字典的关键配置：**

```python
CFG = {
    "architecture": "p2_small",      # 推荐：standard/lska_ca/p2_small/bifpn
    "attention_module": "CBAM",      # LSKA/CA/CBAM/ECA/None
    "epochs": 150,
    "focal_loss": True,              # 闪络类别平衡
    "slide_loss": True,              # 小目标聚焦
    "ema_enabled": True,             # 指数移动平均
    "flashover_cls_weight": 3.0,     # 闪络损失权重
    "publish_after_train": True,     # 训练完自动发布到edge/models/
}
```

### 4.3 启动GUI检测系统

```bash
python window_main.py
```

操作顺序：加载模型(.pt/.onnx/.engine) → 上传图片/选择文件夹/打开摄像头 → 开始检测

### 4.4 启动云端API

```bash
cd cloud
python cloud_api.py
# 服务运行在 http://localhost:5000
# 端点: /health /upload /models /models/current /feedback/stats
```

### 4.5 边缘端推理

```bash
# 在 Jetson Nano 上
cd edge
python edge_inference.py          # 默认摄像头
python edge_inference.py video.mp4  # 本地视频
```

### 4.6 性能基准测试

```bash
cd edge
python benchmark.py --model models/current.onnx
python benchmark.py --model models/current.onnx --trt-model models/current.engine
```

### 4.7 数据集分析

```bash
cd cloud
python dataset_analysis.py
# 输出：类别分布、边界框尺寸、小目标占比、数据不平衡比
```

### 4.8 Jetson 远程部署

```bash
cd deploy
bash deploy.sh <jetson-ip> <jetson-user>
# 自动完成：同步代码 → 安装依赖 → 配置systemd → 启动服务
```

---

## 五、数据流与工作流程

```
┌─────────────────────────────────────────────────────────┐
│                     云端训练 (PC/服务器)                   │
│                                                         │
│  数据集(cloud/data/) → train_better.py → 训练 → 评估     │
│      ↓                                                  │
│  ONNX导出 → 发布到 edge/models/current.onnx              │
│      ↓                                                  │
│  cloud_api.py 接收边缘端反馈 → 增量训练 → 循环迭代         │
└─────────────────────────────────────────────────────────┘
                            ↓ (scp/同步)
┌─────────────────────────────────────────────────────────┐
│                   边缘推理 (Jetson Nano)                  │
│                                                         │
│  加载 current.onnx → 摄像头/视频输入 → 实时检测           │
│      ↓                                                  │
│  检测结果 → 本地缓存 → 定期上传云端(/upload)               │
│      ↓                                                  │
│  模型文件更新检测(10s间隔) → 自动热加载新模型              │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   GUI桌面端 (Windows)                     │
│                                                         │
│  window_main.py → 加载模型(.pt/.onnx/.engine)             │
│      ↓                                                  │
│  单张/批量/视频/摄像头 四种检测模式                        │
│      ↓                                                  │
│  可视化标注 + 检测报告 + YOLO格式导出                      │
└─────────────────────────────────────────────────────────┘
```

---

## 六、常见问题

**Q: 训练时显存不足？**
A: 减小 `batch_size`（默认8→4→2），或设置 `device='cpu'`

**Q: 怎么换用不同的注意力模块？**
A: 修改 `train_better.py` 中 `CFG["attention_module"]` 为 `"CBAM"/"ECA"/"LSKA"/"CA"/"None"`

**Q: 怎么切换不同模型架构？**
A: 修改 `CFG["architecture"]` 为 `"p2_small"/"bifpn"/"lska_ca"/"standard"`

**Q: 边缘端怎么知道模型更新了？**
A: `edge_inference.py` 每10秒检查 `edge/models/current.onnx` 的时间戳，自动热加载

**Q: ONNX模型加载报错？**
A: 确认 `pip install onnxruntime` 或 `pip install onnxruntime-gpu`

---

## 七、已删除的冗余文件清单

以下文件已在清理中删除（释放约36MB）：

| 删除内容 | 原因 |
|---------|------|
| `edge/__pycache__/` | Python 字节码缓存 |
| `edge_cache/`, `edge/cache/`, `edge/edge_cache/` | 空运行时缓存目录 |
| `cloud/feedback_data/`, `edge/feedback_cache/` | 空反馈缓存 |
| `cloud/window_main.py` | 旧版副本（根目录已有新版） |
| `cloud/yolov8n.pt` | 与根目录重复 |
| `edge/models/current.onnx.*.bak`（2个） | 旧模型备份（约24MB） |
| `基于边缘计算的绝缘子缺陷检测系统.doc` | 旧版报告（已有.docx） |
| `improve_report.py` | 一次性报告生成脚本 |
